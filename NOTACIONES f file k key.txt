NOTACIONES
f = file
k = key (clave)
k (minúscula) = clave privada
K (mayúscula) = clave pública
u = usuario
p = Contraseña
c = archivo cifrado

Usar SHA-3 (256) para el hasheo de la contraseña, pillar los primeros 128 bits para cifrar y el resto para autenticar el usuario.

E = funcion de encriptado
D = funcion de desencriptado
Superíndice: algiritmo utilizado
Subíidncie: clave utilizada

En el capíutlo de seguridad de la memoria se tiene que hablar de manera genérica/ambigüa. Ejemplos de código solo en la sección de desarrollo.

Ni de coña, pero posiblemente usar 2FA

Empezar con RSA 1024, intentar que llegue a 2048

CODIFICAR LA CLAVE EN CLARO EN BASE64 DESDE EL CLIENTE



DEFINICIÓN DE LA API
- "/login": POST: Usuario, Contraseña
- "/signin": POST: usuario Contraseña (no todavía)
- "/username/{body}": GET: Usuario 
- "/logout": POST: userId
- "/files" GET: userId, Ruta
- "/file" GET: userId, Ruta
- "/file" POST: userId, Archivo,
{
	nombre,
	id,
	fecha,
	...
	clave,
	ruta,
	autorizaciones,
}
- "/file" PUT: userId, Archivo,
{
	nombre,
	id,
	fecha,
	...
	clave,
	ruta
	autorizaciones,
}
- "/file" DELETE: userId, Ruta
- "/folder" POST: userId, Ruta, Nombre
- "/folder" DELETE: userId, Ruta
- "/folder" POST: userId, Ruta, Nombre
